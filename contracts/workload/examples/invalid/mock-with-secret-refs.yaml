# INVALID FIXTURE. This document must never validate.
#
# Refused for: a mock-llm workload declares a secret reference.
#
# A fixture replayer reaches nothing that needs a credential. A mock that asks
# for one is either misdescribing itself or is a continuous-integration workload
# pointed at a real secret, and both are worth refusing. This rule is semantic:
# the secret entry below is structurally perfect, and the schema accepts it.
#
# The secret named here does not exist.
apiVersion: inferops.io/v1alpha1
kind: WorkloadContract

metadata:
  name: support-assistant-mock
  version: 0.1.0
  owner: team-platform-demo
  description: Invalid fixture. A mock that asks for a credential.

spec:
  profile: mock-llm
  environment: ci

  model:
    servingCapability: inferops-mock-serving
    modelRef: mock-fixed-fixture
    runtimeProfile: resource-conscious

  resources:
    cpu: 250m
    memory: 128Mi
    accelerator:
      type: none
      count: 0

  scaling:
    minimumReplicas: 1
    maximumReplicas: 1

  integrations:
    telemetry:
      capabilityRef: platform-telemetry
      required: true

  security:
    dataClassification: public
    secretRefs:
      - name: model-registry-token
        provider: kubernetes-secret
        reference: inferops-serving/model-registry#token
        owner: team-platform-demo
        rotation: owner-managed

  attribution:
    tenant: demo
    costCenter: demo-cost-center

  evidence:
    runbookRef: docs/serving/mock-and-real-boundary.md

  mockLlm:
    ciOnly: true
    determinism: fixed-fixture
    fixtureRef: contracts/workload/fixtures/mock-llm-chat-completion.response.json
