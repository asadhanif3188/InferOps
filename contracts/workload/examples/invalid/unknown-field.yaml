# INVALID FIXTURE. This document must never validate.
#
# Refused for: a misspelled field name under spec.scaling.
#
# This is the fixture behind the extension rule the contract document argues for.
# With unknown fields accepted for forward compatibility, "maxiumumReplicas" is a
# silent single-replica deployment. With them rejected, it is a validation error
# with the field name in it. The second is the cheaper failure, and this fixture
# is what keeps that trade from being quietly reversed.
apiVersion: inferops.io/v1alpha1
kind: WorkloadContract

metadata:
  name: support-assistant
  version: 0.1.0
  owner: team-platform-demo
  description: Invalid fixture. Carries a misspelled scaling field.

spec:
  profile: synchronous-llm
  environment: local

  model:
    servingCapability: inferops-native-serving
    modelRef: qwen3-1-7b-q8-0
    runtimeProfile: resource-conscious

  resources:
    cpu: "6"
    memory: 3Gi
    accelerator:
      type: none
      count: 0

  scaling:
    minimumReplicas: 1
    maximumReplicas: 1
    maxiumumReplicas: 8

  integrations:
    telemetry:
      capabilityRef: platform-telemetry
      required: true

  security:
    dataClassification: internal
    secretRefs: []

  attribution:
    tenant: demo
    costCenter: demo-cost-center

  evidence:
    runbookRef: docs/serving/feasibility-workflow.md

  synchronousLlm:
    runtime:
      imageReference: ghcr.io/ggml-org/llama.cpp@sha256:100de626bdc5b7df898c12561eefaf557019d2746d5fc8d3f4d7fd24e15ad384
    modelArtifact:
      repository: Qwen/Qwen3-1.7B-GGUF
      revision: 90862c4b9d2787eaed51d12237eafdfe7c5f6077
      file: Qwen3-1.7B-Q8_0.gguf
      sizeBytes: 1834426016
      sha256: sha256:061b54daade076b5d3362dac252678d17da8c68f07560be70818cace6590cb1a
