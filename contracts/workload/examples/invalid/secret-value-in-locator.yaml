# INVALID FIXTURE. This document must never validate.
#
# Refused for: two secret entries whose locator is shaped like a credential.
#
# Both strings below match the schema's locator pattern, so the structural layer
# accepts them. The semantic layer refuses them, one by each of its two tests:
#
#   entry 0  begins with a published credential prefix
#   entry 1  contains an opaque high-entropy segment with mixed case and digits
#
# NEITHER STRING IS A CREDENTIAL. AKIAIOSFODNN7EXAMPLE is the placeholder key
# identifier published in AWS's own documentation and grants nothing. The second
# is a fixed synthetic string written for this fixture; it authenticates against
# nothing and is committed only so that the shape it demonstrates stays tested.
#
# What this check does not catch is measured and published in the contract
# document. It is a heuristic over shape, not a proof.
apiVersion: inferops.io/v1alpha1
kind: WorkloadContract

metadata:
  name: support-assistant-dev
  version: 0.1.0
  owner: team-platform-demo
  description: Invalid fixture. Locator fields shaped like pasted credentials.

spec:
  profile: synchronous-llm
  environment: dev

  model:
    servingCapability: inferops-native-serving
    modelRef: qwen3-1-7b-q8-0
    runtimeProfile: resource-conscious

  resources:
    cpu: "6"
    memory: 3Gi
    accelerator:
      type: none
      count: 0

  scaling:
    minimumReplicas: 1
    maximumReplicas: 1

  integrations:
    telemetry:
      capabilityRef: platform-telemetry
      required: true

  security:
    dataClassification: confidential
    secretRefs:
      - name: object-store-access-key
        provider: external-secret
        reference: AKIAIOSFODNN7EXAMPLE
        owner: team-platform-demo
        rotation: owner-managed
      - name: telemetry-ingest-key
        provider: external-secret
        reference: inferops/telemetry/Zx4Kq9TbLm2Rd7Wf1Hs3Nv8Yc6Ej0Pa
        owner: team-platform-demo
        rotation: platform-managed

  attribution:
    tenant: demo
    costCenter: demo-cost-center

  evidence:
    runbookRef: docs/serving/feasibility-workflow.md

  synchronousLlm:
    runtime:
      imageReference: ghcr.io/ggml-org/llama.cpp@sha256:100de626bdc5b7df898c12561eefaf557019d2746d5fc8d3f4d7fd24e15ad384
    modelArtifact:
      repository: Qwen/Qwen3-1.7B-GGUF
      revision: 90862c4b9d2787eaed51d12237eafdfe7c5f6077
      file: Qwen3-1.7B-Q8_0.gguf
      sizeBytes: 1834426016
      sha256: sha256:061b54daade076b5d3362dac252678d17da8c68f07560be70818cace6590cb1a
