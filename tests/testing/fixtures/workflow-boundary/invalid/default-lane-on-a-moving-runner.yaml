# inferops-lane: default-checks
#
# Refused: ubuntu-latest moves, and a lane whose runner moves cannot attribute a failure.

name: checks

on:
  pull_request:

permissions:
  contents: read

jobs:
  infrastructure:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1
      - name: The step the rule refuses
        run: helm lint charts/inferops-llm --strict
