# V1-S3-007-PR2 query evaluation — every published query, over every committed scenario

Generated by `python -m tools.telemetry_correlation --evidence` and compared
against this file by `tests/telemetry/test_telemetry_correlation.py`. Editing it
by hand makes that suite fail, which is the point: it is a record of what the
queries returned, not a description of what somebody expected them to.

**Evidence class `local-static`, and every store below is synthetic.** No
Prometheus parsed, loaded, or evaluated any expression here. Every result was
produced by the declared PromQL subset evaluator in
[`tools/telemetry_correlation/evaluate.py`](../../../tools/telemetry_correlation/evaluate.py)
over fixture series written by hand in
[`tests/telemetry/fixtures/correlation/`](../../../tests/telemetry/fixtures/correlation/).
Nothing was scraped, no endpoint was contacted, no collector exists, and this
chart has never been installed.

A query absent from a scenario is a query the scenario's profile does not render.
A query present with no rows returned an empty vector, which is a different
answer and is written differently.

## The queries

Each expression is written once here and referred to by identifier below.

### `build-identity-per-replica` — answerable-once-collected

> Which build, capability, release, model revision, runtime, and image digest is each replica running, and in which environment?

```promql
inferops_build_info
```

### `request-throughput-by-workload-model-and-outcome` — answerable-once-collected

> How much work is each workload doing on each model, and how much of it is succeeding?

```promql
sum by (inferops_workload_id, inferops_model_id, inferops_outcome) (rate(inferops_inference_requests_total[5m]))
```

### `error-rate-by-workload-and-code` — answerable-once-collected

> Which canonical error is each workload producing, and how often?

```promql
sum by (inferops_workload_id, inferops_error_code) (rate(inferops_inference_errors_total[5m]))
```

### `request-latency-p95-by-workload-and-model` — answerable-once-collected

> How long does a request take at the tail, for each workload and model?

```promql
histogram_quantile(0.95, sum by (inferops_workload_id, inferops_model_id, le) (rate(inferops_inference_request_duration_seconds_bucket[5m])))
```

### `in-flight-requests-by-replica` — answerable-once-collected

> How much work is in flight on each replica right now?

```promql
sum by (k8s_namespace, k8s_component, instance, inferops_workload_id, inferops_model_id) (inferops_inference_requests_in_flight)
```

### `throughput-joined-to-build-identity` — answerable-once-collected

> Which build produced this throughput, on which replica, in which environment, against which model revision and runtime image?

```promql
sum by (job, instance, inferops_workload_id, inferops_model_id, inferops_outcome) (rate(inferops_inference_requests_total[5m])) * on (job, instance) group_left (service_version, deployment_environment, inferops_release_id, inferops_capability_id, inferops_adapter_kind, inferops_model_revision, inferops_runtime_id, inferops_runtime_image_digest) inferops_build_info
```

### `tokens-by-direction-from-the-api` — answerable-once-collected

> How many tokens is each model consuming and producing, as the API counts them?

```promql
sum by (inferops_model_id, inferops_token_direction) (rate(inferops_inference_tokens_total[5m]))
```

### `tokens-by-direction-from-the-runtime` — answerable-once-collected

> How many tokens is the serving runtime reporting, in the same shape as the API's counter?

```promql
sum by (inferops_model_id, inferops_token_direction) (rate(inferops:inference_tokens:runtime_total[5m]))
```

### `api-and-runtime-in-flight-side-by-side` — answerable-once-collected

> How far apart are the API's view of in-flight work and the runtime's?

```promql
sum by (inferops_workload_id, inferops_model_id) (inferops_inference_requests_in_flight) - on (inferops_workload_id, inferops_model_id) sum by (inferops_workload_id, inferops_model_id) (inferops:inference_requests_in_flight:runtime)
```

### `scrape-target-health-by-job` — answerable-once-collected

> For each job, how many of its targets answered the last scrape?

```promql
inferops:scrape_targets_up:sum / inferops:scrape_targets:count
```

### `platform-api-scrape-job-absent` — answerable-once-collected

> Did the platform-api job discover no pod at all?

```promql
inferops:scrape_job_absent:platform_api
```

### `serving-runtime-scrape-job-absent` — answerable-once-collected

> Did the serving-runtime job discover no pod at all?

```promql
inferops:scrape_job_absent:serving_runtime
```

### `identity-absent-on-a-target-that-answered` — answerable-once-collected

> Is there an API target that is up and publishing no identity?

```promql
inferops:build_info_absent:platform_api
```

### `model-readiness-absent` — answerable-once-collected

> Is model readiness missing?

```promql
inferops:model_ready_absent:platform_api
```

### `model-readiness-by-workload` — not-answerable-nothing-emits

> Is each workload's model ready to serve?

```promql
inferops_model_ready
```

### `model-load-duration-p95` — not-answerable-nothing-emits

> How long does loading a model take, at the tail?

```promql
histogram_quantile(0.95, sum by (inferops_model_id, inferops_runtime_id, le) (rate(inferops_model_load_duration_seconds_bucket[5m])))
```

### `queue-wait-p95` — not-answerable-nothing-emits

> How long does a request wait before the runtime starts on it?

```promql
histogram_quantile(0.95, sum by (inferops_workload_id, inferops_model_id, le) (rate(inferops_inference_queue_duration_seconds_bucket[5m])))
```

### `runtime-deferred-requests` — answerable-once-collected

> Is the serving runtime deferring work?

```promql
sum by (inferops_workload_id, inferops_model_id) (inferops:inference_requests_deferred:runtime)
```

### `api-process-cpu-by-replica` — answerable-once-collected

> How much CPU is each API replica's process using?

```promql
sum by (k8s_namespace, k8s_component, instance) (rate(inferops_process_cpu_seconds_total[5m]))
```

### `api-process-memory-by-replica` — not-answerable-nothing-emits

> How much memory is each API replica's process using?

```promql
sum by (k8s_namespace, k8s_component, instance) (inferops_process_resident_memory_bytes)
```

### `readiness-check-failures-by-component` — answerable-once-collected

> Which readiness component is refusing traffic, and how often?

```promql
sum by (inferops_workload_id, inferops_component) (rate(inferops_readiness_check_failures_total[5m]))
```

### `container-and-pod-resource-use` — no-source

> How much CPU and memory is each container using against its declared request and limit?

No expression. There is no series to select: a kubelet cAdvisor scrape job, or metrics-server, or kube-state-metrics -- each of which is a cluster add-on this chart does not own and this configuration does not install would be needed first.

### `pod-phase-restarts-and-container-readiness` — no-source

> Which pods are restarting, and which containers are not ready?

No expression. There is no series to select: kube-state-metrics would be needed first.

## The results

### `healthy-real-two-replicas` — real profile

Two API replicas and two runtime replicas, every target answering, every emitted metric present and every identity published. Every answerable query returns the row it claims to, and the identity join carries the build labels onto the throughput of both replicas.

#### `api-and-runtime-in-flight-side-by-side`

```
{inferops_model_id="qwen3-1-7b-q8-0", inferops_workload_id="support-assistant"} 2
```

#### `api-process-cpu-by-replica`

```
{instance="inferops-inferops-llm-api-6d4f8-2xkqp", k8s_component="platform-api", k8s_namespace="inferops-platform"} 0.1
{instance="inferops-inferops-llm-api-6d4f8-9wbnt", k8s_component="platform-api", k8s_namespace="inferops-platform"} 0.05
```

#### `api-process-memory-by-replica`

Empty. Always empty. The only per-process memory figure the standard library exposes is /proc/self/statm, and a module under src/inferops may not read a path -- the dependency-boundary property enforces it so that the distribution stays usable from a wheel.

#### `build-identity-per-replica`

```
{__name__="inferops_build_info", deployment_environment="dev", inferops_adapter_kind="real", inferops_capability_id="inferops-native-serving", inferops_model_id="qwen3-1-7b-q8-0", inferops_model_revision="90862c4b9d2787eaed51d12237eafdfe7c5f6077", inferops_release_id="inferops", inferops_runtime_id="llama-cpp-server", inferops_runtime_image_digest="sha256:100de626bdc5b7df898c12561eefaf557019d2746d5fc8d3f4d7fd24e15ad384", instance="inferops-inferops-llm-api-6d4f8-2xkqp", job="inferops-inferops-llm-platform-api", k8s_component="platform-api", k8s_namespace="inferops-platform", service_version="0.1.0"} 1
{__name__="inferops_build_info", deployment_environment="dev", inferops_adapter_kind="real", inferops_capability_id="inferops-native-serving", inferops_model_id="qwen3-1-7b-q8-0", inferops_model_revision="90862c4b9d2787eaed51d12237eafdfe7c5f6077", inferops_release_id="inferops", inferops_runtime_id="llama-cpp-server", inferops_runtime_image_digest="sha256:100de626bdc5b7df898c12561eefaf557019d2746d5fc8d3f4d7fd24e15ad384", instance="inferops-inferops-llm-api-6d4f8-9wbnt", job="inferops-inferops-llm-platform-api", k8s_component="platform-api", k8s_namespace="inferops-platform", service_version="0.1.0"} 1
```

#### `error-rate-by-workload-and-code`

```
{inferops_error_code="model-not-ready", inferops_workload_id="support-assistant"} 0.15
```

#### `identity-absent-on-a-target-that-answered`

Empty. At least one API target in this release's namespace published an identity.

#### `in-flight-requests-by-replica`

```
{inferops_model_id="qwen3-1-7b-q8-0", inferops_workload_id="support-assistant", instance="inferops-inferops-llm-api-6d4f8-2xkqp", k8s_component="platform-api", k8s_namespace="inferops-platform"} 3
{inferops_model_id="qwen3-1-7b-q8-0", inferops_workload_id="support-assistant", instance="inferops-inferops-llm-api-6d4f8-9wbnt", k8s_component="platform-api", k8s_namespace="inferops-platform"} 2
```

#### `model-load-duration-p95`

Empty. Always empty. Same emitter, same absence: the adapter is not instrumented, and llama-server publishes no load-duration series either.

#### `model-readiness-absent`

```
{__name__="inferops:model_ready_absent:platform_api", k8s_component="platform-api", k8s_namespace="inferops-platform"} 1
```

#### `model-readiness-by-workload`

Empty. Always empty. The catalog assigns inferops_model_ready to the serving-runtime adapter and marks it not emitted; nothing puts the series on any endpoint, so a collector would change nothing.

#### `platform-api-scrape-job-absent`

Empty. The job has at least one target. Absence of this series is the healthy state, and presence of it reading 1 is the failure.

#### `queue-wait-p95`

Empty. Always empty. The wait is the serving-runtime adapter's measurement and the adapter is not instrumented.

#### `readiness-check-failures-by-component`

```
{inferops_component="serving-adapter", inferops_workload_id="support-assistant"} 0.01
```

#### `request-latency-p95-by-workload-and-model`

```
{inferops_model_id="qwen3-1-7b-q8-0", inferops_workload_id="support-assistant"} 57.5
```

#### `request-throughput-by-workload-model-and-outcome`

```
{inferops_model_id="qwen3-1-7b-q8-0", inferops_outcome="server-error", inferops_workload_id="support-assistant"} 0.15
{inferops_model_id="qwen3-1-7b-q8-0", inferops_outcome="success", inferops_workload_id="support-assistant"} 3
```

#### `runtime-deferred-requests`

```
{inferops_model_id="qwen3-1-7b-q8-0", inferops_workload_id="support-assistant"} 4
```

#### `scrape-target-health-by-job`

```
{job="inferops-inferops-llm-platform-api", k8s_component="platform-api", k8s_namespace="inferops-platform"} 1
{job="inferops-inferops-llm-serving-runtime", k8s_component="serving-runtime", k8s_namespace="inferops-platform"} 1
```

#### `serving-runtime-scrape-job-absent`

Empty. The job has at least one target.

#### `throughput-joined-to-build-identity`

```
{deployment_environment="dev", inferops_adapter_kind="real", inferops_capability_id="inferops-native-serving", inferops_model_id="qwen3-1-7b-q8-0", inferops_model_revision="90862c4b9d2787eaed51d12237eafdfe7c5f6077", inferops_outcome="server-error", inferops_release_id="inferops", inferops_runtime_id="llama-cpp-server", inferops_runtime_image_digest="sha256:100de626bdc5b7df898c12561eefaf557019d2746d5fc8d3f4d7fd24e15ad384", inferops_workload_id="support-assistant", instance="inferops-inferops-llm-api-6d4f8-2xkqp", job="inferops-inferops-llm-platform-api", service_version="0.1.0"} 0.1
{deployment_environment="dev", inferops_adapter_kind="real", inferops_capability_id="inferops-native-serving", inferops_model_id="qwen3-1-7b-q8-0", inferops_model_revision="90862c4b9d2787eaed51d12237eafdfe7c5f6077", inferops_outcome="server-error", inferops_release_id="inferops", inferops_runtime_id="llama-cpp-server", inferops_runtime_image_digest="sha256:100de626bdc5b7df898c12561eefaf557019d2746d5fc8d3f4d7fd24e15ad384", inferops_workload_id="support-assistant", instance="inferops-inferops-llm-api-6d4f8-9wbnt", job="inferops-inferops-llm-platform-api", service_version="0.1.0"} 0.05
{deployment_environment="dev", inferops_adapter_kind="real", inferops_capability_id="inferops-native-serving", inferops_model_id="qwen3-1-7b-q8-0", inferops_model_revision="90862c4b9d2787eaed51d12237eafdfe7c5f6077", inferops_outcome="success", inferops_release_id="inferops", inferops_runtime_id="llama-cpp-server", inferops_runtime_image_digest="sha256:100de626bdc5b7df898c12561eefaf557019d2746d5fc8d3f4d7fd24e15ad384", inferops_workload_id="support-assistant", instance="inferops-inferops-llm-api-6d4f8-2xkqp", job="inferops-inferops-llm-platform-api", service_version="0.1.0"} 2
{deployment_environment="dev", inferops_adapter_kind="real", inferops_capability_id="inferops-native-serving", inferops_model_id="qwen3-1-7b-q8-0", inferops_model_revision="90862c4b9d2787eaed51d12237eafdfe7c5f6077", inferops_outcome="success", inferops_release_id="inferops", inferops_runtime_id="llama-cpp-server", inferops_runtime_image_digest="sha256:100de626bdc5b7df898c12561eefaf557019d2746d5fc8d3f4d7fd24e15ad384", inferops_workload_id="support-assistant", instance="inferops-inferops-llm-api-6d4f8-9wbnt", job="inferops-inferops-llm-platform-api", service_version="0.1.0"} 1
```

#### `tokens-by-direction-from-the-api`

```
{inferops_model_id="qwen3-1-7b-q8-0", inferops_token_direction="input"} 15
{inferops_model_id="qwen3-1-7b-q8-0", inferops_token_direction="output"} 7.5
```

#### `tokens-by-direction-from-the-runtime`

```
{inferops_model_id="qwen3-1-7b-q8-0", inferops_token_direction="input"} 14
{inferops_model_id="qwen3-1-7b-q8-0", inferops_token_direction="output"} 10
```

Not evaluated in this scenario: `container-and-pod-resource-use`, `pod-phase-restarts-and-container-readiness`. Each either has no expression to run or is not rendered under the real profile.

### `serving-runtime-not-discovered` — real profile

The runtime job's selector matched no pod, so there is no up series for it at all. The API side is unchanged and healthy. This is the failure that reads as a healthy quiet system in every query that groups by job, and the scenario is what proves the absence rule separates it.

#### `api-and-runtime-in-flight-side-by-side`

Empty. One of the two sides is missing. A one-to-one match drops every element with no counterpart, so a runtime job that discovered nothing makes this empty rather than equal to the API's figure.

#### `api-process-cpu-by-replica`

Empty. No API target is being scraped.

#### `api-process-memory-by-replica`

Empty. Always empty. The only per-process memory figure the standard library exposes is /proc/self/statm, and a module under src/inferops may not read a path -- the dependency-boundary property enforces it so that the distribution stays usable from a wheel.

#### `build-identity-per-replica`

```
{__name__="inferops_build_info", deployment_environment="dev", inferops_adapter_kind="real", inferops_capability_id="inferops-native-serving", inferops_model_id="qwen3-1-7b-q8-0", inferops_model_revision="90862c4b9d2787eaed51d12237eafdfe7c5f6077", inferops_release_id="inferops", inferops_runtime_id="llama-cpp-server", inferops_runtime_image_digest="sha256:100de626bdc5b7df898c12561eefaf557019d2746d5fc8d3f4d7fd24e15ad384", instance="inferops-inferops-llm-api-6d4f8-2xkqp", job="inferops-inferops-llm-platform-api", k8s_component="platform-api", k8s_namespace="inferops-platform", service_version="0.1.0"} 1
```

#### `error-rate-by-workload-and-code`

Empty. No error has been counted. It does not mean the system is healthy: a target that is not scraped at all produces the same empty result, which is what the collection-health queries exist to separate.

#### `identity-absent-on-a-target-that-answered`

Empty. At least one API target in this release's namespace published an identity.

#### `in-flight-requests-by-replica`

```
{inferops_model_id="qwen3-1-7b-q8-0", inferops_workload_id="support-assistant", instance="inferops-inferops-llm-api-6d4f8-2xkqp", k8s_component="platform-api", k8s_namespace="inferops-platform"} 3
```

#### `model-load-duration-p95`

Empty. Always empty. Same emitter, same absence: the adapter is not instrumented, and llama-server publishes no load-duration series either.

#### `model-readiness-absent`

```
{__name__="inferops:model_ready_absent:platform_api", k8s_component="platform-api", k8s_namespace="inferops-platform"} 1
```

#### `model-readiness-by-workload`

Empty. Always empty. The catalog assigns inferops_model_ready to the serving-runtime adapter and marks it not emitted; nothing puts the series on any endpoint, so a collector would change nothing.

#### `platform-api-scrape-job-absent`

Empty. The job has at least one target. Absence of this series is the healthy state, and presence of it reading 1 is the failure.

#### `queue-wait-p95`

Empty. Always empty. The wait is the serving-runtime adapter's measurement and the adapter is not instrumented.

#### `readiness-check-failures-by-component`

Empty. No readiness check has failed since the process started, or no API target is being scraped.

#### `request-latency-p95-by-workload-and-model`

Empty. No observation in the window. A quantile over a histogram with no +Inf bucket is NaN rather than empty, and a fixture proves that case separately.

#### `request-throughput-by-workload-model-and-outcome`

```
{inferops_model_id="qwen3-1-7b-q8-0", inferops_outcome="success", inferops_workload_id="support-assistant"} 2
```

#### `runtime-deferred-requests`

Empty. The runtime job discovered nothing, or no collector is evaluating the recording rules.

#### `scrape-target-health-by-job`

```
{job="inferops-inferops-llm-platform-api", k8s_component="platform-api", k8s_namespace="inferops-platform"} 1
```

#### `serving-runtime-scrape-job-absent`

```
{__name__="inferops:scrape_job_absent:serving_runtime", job="inferops-inferops-llm-serving-runtime"} 1
```

#### `throughput-joined-to-build-identity`

```
{deployment_environment="dev", inferops_adapter_kind="real", inferops_capability_id="inferops-native-serving", inferops_model_id="qwen3-1-7b-q8-0", inferops_model_revision="90862c4b9d2787eaed51d12237eafdfe7c5f6077", inferops_outcome="success", inferops_release_id="inferops", inferops_runtime_id="llama-cpp-server", inferops_runtime_image_digest="sha256:100de626bdc5b7df898c12561eefaf557019d2746d5fc8d3f4d7fd24e15ad384", inferops_workload_id="support-assistant", instance="inferops-inferops-llm-api-6d4f8-2xkqp", job="inferops-inferops-llm-platform-api", service_version="0.1.0"} 2
```

#### `tokens-by-direction-from-the-api`

Empty. No completion has been counted since the process started.

#### `tokens-by-direction-from-the-runtime`

Empty. Either the runtime job discovered nothing, or no collector is evaluating the chart's recording rules, in which case the recorded series does not exist at all.

Not evaluated in this scenario: `container-and-pod-resource-use`, `pod-phase-restarts-and-container-readiness`. Each either has no expression to run or is not rendered under the real profile.

### `api-target-up-without-identity` — real profile

An API target that answers a scrape and publishes no inferops_build_info. up reads 1 and the target is useless: the identity join returns nothing, and the identity-absence rule is what says why.

#### `api-and-runtime-in-flight-side-by-side`

```
{inferops_model_id="qwen3-1-7b-q8-0", inferops_workload_id="support-assistant"} 1
```

#### `api-process-cpu-by-replica`

Empty. No API target is being scraped.

#### `api-process-memory-by-replica`

Empty. Always empty. The only per-process memory figure the standard library exposes is /proc/self/statm, and a module under src/inferops may not read a path -- the dependency-boundary property enforces it so that the distribution stays usable from a wheel.

#### `build-identity-per-replica`

Empty. No API target published an identity. That is either a job that discovered nothing, which inferops:scrape_job_absent:platform_api separates, or a target that answered a scrape and published no identity, which inferops:build_info_absent:platform_api separates.

#### `error-rate-by-workload-and-code`

Empty. No error has been counted. It does not mean the system is healthy: a target that is not scraped at all produces the same empty result, which is what the collection-health queries exist to separate.

#### `identity-absent-on-a-target-that-answered`

```
{__name__="inferops:build_info_absent:platform_api", k8s_component="platform-api", k8s_namespace="inferops-platform"} 1
```

#### `in-flight-requests-by-replica`

```
{inferops_model_id="qwen3-1-7b-q8-0", inferops_workload_id="support-assistant", instance="inferops-inferops-llm-api-6d4f8-2xkqp", k8s_component="platform-api", k8s_namespace="inferops-platform"} 3
```

#### `model-load-duration-p95`

Empty. Always empty. Same emitter, same absence: the adapter is not instrumented, and llama-server publishes no load-duration series either.

#### `model-readiness-absent`

```
{__name__="inferops:model_ready_absent:platform_api", k8s_component="platform-api", k8s_namespace="inferops-platform"} 1
```

#### `model-readiness-by-workload`

Empty. Always empty. The catalog assigns inferops_model_ready to the serving-runtime adapter and marks it not emitted; nothing puts the series on any endpoint, so a collector would change nothing.

#### `platform-api-scrape-job-absent`

Empty. The job has at least one target. Absence of this series is the healthy state, and presence of it reading 1 is the failure.

#### `queue-wait-p95`

Empty. Always empty. The wait is the serving-runtime adapter's measurement and the adapter is not instrumented.

#### `readiness-check-failures-by-component`

Empty. No readiness check has failed since the process started, or no API target is being scraped.

#### `request-latency-p95-by-workload-and-model`

Empty. No observation in the window. A quantile over a histogram with no +Inf bucket is NaN rather than empty, and a fixture proves that case separately.

#### `request-throughput-by-workload-model-and-outcome`

```
{inferops_model_id="qwen3-1-7b-q8-0", inferops_outcome="success", inferops_workload_id="support-assistant"} 2
```

#### `runtime-deferred-requests`

Empty. The runtime job discovered nothing, or no collector is evaluating the recording rules.

#### `scrape-target-health-by-job`

```
{job="inferops-inferops-llm-platform-api", k8s_component="platform-api", k8s_namespace="inferops-platform"} 1
{job="inferops-inferops-llm-serving-runtime", k8s_component="serving-runtime", k8s_namespace="inferops-platform"} 1
```

#### `serving-runtime-scrape-job-absent`

Empty. The job has at least one target.

#### `throughput-joined-to-build-identity`

Empty. Either no request has been served, or a target answered a scrape and published no identity: the join finds no counterpart and drops every element rather than returning a partial answer. inferops:build_info_absent:platform_api is what tells the two apart.

#### `tokens-by-direction-from-the-api`

Empty. No completion has been counted since the process started.

#### `tokens-by-direction-from-the-runtime`

Empty. Either the runtime job discovered nothing, or no collector is evaluating the chart's recording rules, in which case the recorded series does not exist at all.

Not evaluated in this scenario: `container-and-pod-resource-use`, `pod-phase-restarts-and-container-readiness`. Each either has no expression to run or is not rendered under the real profile.

### `every-target-down` — real profile

Both jobs have targets and none of them answered. The target count stays at its full value and the up sum reads zero, which is the distinction a count over a filtered vector would have lost.

#### `api-and-runtime-in-flight-side-by-side`

Empty. One of the two sides is missing. A one-to-one match drops every element with no counterpart, so a runtime job that discovered nothing makes this empty rather than equal to the API's figure.

#### `api-process-cpu-by-replica`

Empty. No API target is being scraped.

#### `api-process-memory-by-replica`

Empty. Always empty. The only per-process memory figure the standard library exposes is /proc/self/statm, and a module under src/inferops may not read a path -- the dependency-boundary property enforces it so that the distribution stays usable from a wheel.

#### `build-identity-per-replica`

Empty. No API target published an identity. That is either a job that discovered nothing, which inferops:scrape_job_absent:platform_api separates, or a target that answered a scrape and published no identity, which inferops:build_info_absent:platform_api separates.

#### `error-rate-by-workload-and-code`

Empty. No error has been counted. It does not mean the system is healthy: a target that is not scraped at all produces the same empty result, which is what the collection-health queries exist to separate.

#### `identity-absent-on-a-target-that-answered`

```
{__name__="inferops:build_info_absent:platform_api", k8s_component="platform-api", k8s_namespace="inferops-platform"} 1
```

#### `in-flight-requests-by-replica`

Empty. No API target is being scraped. A replica with nothing in flight reads 0 and is present.

#### `model-load-duration-p95`

Empty. Always empty. Same emitter, same absence: the adapter is not instrumented, and llama-server publishes no load-duration series either.

#### `model-readiness-absent`

```
{__name__="inferops:model_ready_absent:platform_api", k8s_component="platform-api", k8s_namespace="inferops-platform"} 1
```

#### `model-readiness-by-workload`

Empty. Always empty. The catalog assigns inferops_model_ready to the serving-runtime adapter and marks it not emitted; nothing puts the series on any endpoint, so a collector would change nothing.

#### `platform-api-scrape-job-absent`

Empty. The job has at least one target. Absence of this series is the healthy state, and presence of it reading 1 is the failure.

#### `queue-wait-p95`

Empty. Always empty. The wait is the serving-runtime adapter's measurement and the adapter is not instrumented.

#### `readiness-check-failures-by-component`

Empty. No readiness check has failed since the process started, or no API target is being scraped.

#### `request-latency-p95-by-workload-and-model`

Empty. No observation in the window. A quantile over a histogram with no +Inf bucket is NaN rather than empty, and a fixture proves that case separately.

#### `request-throughput-by-workload-model-and-outcome`

Empty. No API target is being scraped, or no request has been served since the process started.

#### `runtime-deferred-requests`

Empty. The runtime job discovered nothing, or no collector is evaluating the recording rules.

#### `scrape-target-health-by-job`

```
{job="inferops-inferops-llm-platform-api", k8s_component="platform-api", k8s_namespace="inferops-platform"} 0
{job="inferops-inferops-llm-serving-runtime", k8s_component="serving-runtime", k8s_namespace="inferops-platform"} 0
```

#### `serving-runtime-scrape-job-absent`

Empty. The job has at least one target.

#### `throughput-joined-to-build-identity`

Empty. Either no request has been served, or a target answered a scrape and published no identity: the join finds no counterpart and drops every element rather than returning a partial answer. inferops:build_info_absent:platform_api is what tells the two apart.

#### `tokens-by-direction-from-the-api`

Empty. No completion has been counted since the process started.

#### `tokens-by-direction-from-the-runtime`

Empty. Either the runtime job discovered nothing, or no collector is evaluating the chart's recording rules, in which case the recorded series does not exist at all.

Not evaluated in this scenario: `container-and-pod-resource-use`, `pod-phase-restarts-and-container-readiness`. Each either has no expression to run or is not rendered under the real profile.

### `mock-single-replica` — mock profile

The mock profile installs one job and no serving runtime. Every runtime query is absent from the result rather than empty in it, and the identity join still carries the adapter kind that says this is a mock.

#### `api-process-cpu-by-replica`

```
{instance="inferops-inferops-llm-api-7c9b4-tm4qd", k8s_component="platform-api", k8s_namespace="inferops-platform"} 0.02
```

#### `api-process-memory-by-replica`

Empty. Always empty. The only per-process memory figure the standard library exposes is /proc/self/statm, and a module under src/inferops may not read a path -- the dependency-boundary property enforces it so that the distribution stays usable from a wheel.

#### `build-identity-per-replica`

```
{__name__="inferops_build_info", deployment_environment="ci", inferops_adapter_kind="mock", inferops_capability_id="inferops-mock-serving", inferops_model_id="mock-fixed-fixture", inferops_model_revision="mock-fixed-fixture", inferops_release_id="inferops", inferops_runtime_id="inferops-mock-serving", inferops_runtime_image_digest="none", instance="inferops-inferops-llm-api-7c9b4-tm4qd", job="inferops-inferops-llm-platform-api", k8s_component="platform-api", k8s_namespace="inferops-platform", service_version="0.1.0"} 1
```

#### `error-rate-by-workload-and-code`

Empty. No error has been counted. It does not mean the system is healthy: a target that is not scraped at all produces the same empty result, which is what the collection-health queries exist to separate.

#### `identity-absent-on-a-target-that-answered`

Empty. At least one API target in this release's namespace published an identity.

#### `in-flight-requests-by-replica`

```
{inferops_model_id="mock-fixed-fixture", inferops_workload_id="support-assistant-mock", instance="inferops-inferops-llm-api-7c9b4-tm4qd", k8s_component="platform-api", k8s_namespace="inferops-platform"} 1
```

#### `model-load-duration-p95`

Empty. Always empty. Same emitter, same absence: the adapter is not instrumented, and llama-server publishes no load-duration series either.

#### `model-readiness-absent`

```
{__name__="inferops:model_ready_absent:platform_api", k8s_component="platform-api", k8s_namespace="inferops-platform"} 1
```

#### `model-readiness-by-workload`

Empty. Always empty. The catalog assigns inferops_model_ready to the serving-runtime adapter and marks it not emitted; nothing puts the series on any endpoint, so a collector would change nothing.

#### `platform-api-scrape-job-absent`

Empty. The job has at least one target. Absence of this series is the healthy state, and presence of it reading 1 is the failure.

#### `queue-wait-p95`

Empty. Always empty. The wait is the serving-runtime adapter's measurement and the adapter is not instrumented.

#### `readiness-check-failures-by-component`

Empty. No readiness check has failed since the process started, or no API target is being scraped.

#### `request-latency-p95-by-workload-and-model`

Empty. No observation in the window. A quantile over a histogram with no +Inf bucket is NaN rather than empty, and a fixture proves that case separately.

#### `request-throughput-by-workload-model-and-outcome`

```
{inferops_model_id="mock-fixed-fixture", inferops_outcome="success", inferops_workload_id="support-assistant-mock"} 1
```

#### `scrape-target-health-by-job`

```
{job="inferops-inferops-llm-platform-api", k8s_component="platform-api", k8s_namespace="inferops-platform"} 1
```

#### `throughput-joined-to-build-identity`

```
{deployment_environment="ci", inferops_adapter_kind="mock", inferops_capability_id="inferops-mock-serving", inferops_model_id="mock-fixed-fixture", inferops_model_revision="mock-fixed-fixture", inferops_outcome="success", inferops_release_id="inferops", inferops_runtime_id="inferops-mock-serving", inferops_runtime_image_digest="none", inferops_workload_id="support-assistant-mock", instance="inferops-inferops-llm-api-7c9b4-tm4qd", job="inferops-inferops-llm-platform-api", service_version="0.1.0"} 1
```

#### `tokens-by-direction-from-the-api`

```
{inferops_model_id="mock-fixed-fixture", inferops_token_direction="input"} 2
{inferops_model_id="mock-fixed-fixture", inferops_token_direction="output"} 1
```

Not evaluated in this scenario: `api-and-runtime-in-flight-side-by-side`, `container-and-pod-resource-use`, `pod-phase-restarts-and-container-readiness`, `runtime-deferred-requests`, `serving-runtime-scrape-job-absent`, `tokens-by-direction-from-the-runtime`. Each either has no expression to run or is not rendered under the mock profile.

### `forbidden-labels-on-the-exposition` — real profile

A target publishing series labelled with a request identifier, a tenant, a pod name, and an HTTP status -- none of which anything in this repository emits, and all of which a component this project did not write could. The collector's labeldrop removes every one of them before any query sees it.

#### `api-and-runtime-in-flight-side-by-side`

Empty. One of the two sides is missing. A one-to-one match drops every element with no counterpart, so a runtime job that discovered nothing makes this empty rather than equal to the API's figure.

#### `api-process-cpu-by-replica`

Empty. No API target is being scraped.

#### `api-process-memory-by-replica`

Empty. Always empty. The only per-process memory figure the standard library exposes is /proc/self/statm, and a module under src/inferops may not read a path -- the dependency-boundary property enforces it so that the distribution stays usable from a wheel.

#### `build-identity-per-replica`

```
{__name__="inferops_build_info", deployment_environment="dev", inferops_adapter_kind="real", inferops_capability_id="inferops-native-serving", inferops_model_id="qwen3-1-7b-q8-0", inferops_model_revision="90862c4b9d2787eaed51d12237eafdfe7c5f6077", inferops_release_id="inferops", inferops_runtime_id="llama-cpp-server", inferops_runtime_image_digest="sha256:100de626bdc5b7df898c12561eefaf557019d2746d5fc8d3f4d7fd24e15ad384", instance="inferops-inferops-llm-api-6d4f8-2xkqp", job="inferops-inferops-llm-platform-api", k8s_component="platform-api", k8s_namespace="inferops-platform", service_version="0.1.0"} 1
```

#### `error-rate-by-workload-and-code`

```
{inferops_error_code="model-not-ready", inferops_workload_id="support-assistant"} 0.1
```

#### `identity-absent-on-a-target-that-answered`

Empty. At least one API target in this release's namespace published an identity.

#### `in-flight-requests-by-replica`

```
{inferops_model_id="qwen3-1-7b-q8-0", inferops_workload_id="support-assistant", instance="inferops-inferops-llm-api-6d4f8-2xkqp", k8s_component="platform-api", k8s_namespace="inferops-platform"} 3
```

#### `model-load-duration-p95`

Empty. Always empty. Same emitter, same absence: the adapter is not instrumented, and llama-server publishes no load-duration series either.

#### `model-readiness-absent`

```
{__name__="inferops:model_ready_absent:platform_api", k8s_component="platform-api", k8s_namespace="inferops-platform"} 1
```

#### `model-readiness-by-workload`

Empty. Always empty. The catalog assigns inferops_model_ready to the serving-runtime adapter and marks it not emitted; nothing puts the series on any endpoint, so a collector would change nothing.

#### `platform-api-scrape-job-absent`

Empty. The job has at least one target. Absence of this series is the healthy state, and presence of it reading 1 is the failure.

#### `queue-wait-p95`

Empty. Always empty. The wait is the serving-runtime adapter's measurement and the adapter is not instrumented.

#### `readiness-check-failures-by-component`

Empty. No readiness check has failed since the process started, or no API target is being scraped.

#### `request-latency-p95-by-workload-and-model`

Empty. No observation in the window. A quantile over a histogram with no +Inf bucket is NaN rather than empty, and a fixture proves that case separately.

#### `request-throughput-by-workload-model-and-outcome`

```
{inferops_model_id="qwen3-1-7b-q8-0", inferops_outcome="success", inferops_workload_id="support-assistant"} 2
```

#### `runtime-deferred-requests`

Empty. The runtime job discovered nothing, or no collector is evaluating the recording rules.

#### `scrape-target-health-by-job`

```
{job="inferops-inferops-llm-platform-api", k8s_component="platform-api", k8s_namespace="inferops-platform"} 1
```

#### `serving-runtime-scrape-job-absent`

```
{__name__="inferops:scrape_job_absent:serving_runtime", job="inferops-inferops-llm-serving-runtime"} 1
```

#### `throughput-joined-to-build-identity`

```
{deployment_environment="dev", inferops_adapter_kind="real", inferops_capability_id="inferops-native-serving", inferops_model_id="qwen3-1-7b-q8-0", inferops_model_revision="90862c4b9d2787eaed51d12237eafdfe7c5f6077", inferops_outcome="success", inferops_release_id="inferops", inferops_runtime_id="llama-cpp-server", inferops_runtime_image_digest="sha256:100de626bdc5b7df898c12561eefaf557019d2746d5fc8d3f4d7fd24e15ad384", inferops_workload_id="support-assistant", instance="inferops-inferops-llm-api-6d4f8-2xkqp", job="inferops-inferops-llm-platform-api", service_version="0.1.0"} 2
```

#### `tokens-by-direction-from-the-api`

Empty. No completion has been counted since the process started.

#### `tokens-by-direction-from-the-runtime`

Empty. Either the runtime job discovered nothing, or no collector is evaluating the chart's recording rules, in which case the recorded series does not exist at all.

Not evaluated in this scenario: `container-and-pod-resource-use`, `pod-phase-restarts-and-container-readiness`. Each either has no expression to run or is not rendered under the real profile.
