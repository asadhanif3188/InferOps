# TEMPLATE — experiment record

This file is a **template**, not evidence. It records nothing and proves nothing.
Copy it to a filled record beside the area it belongs to — for example
`docs/proof/serving/v1-sN-0NN-prN-<slug>.md` — and fill it in. Do not edit this file
to hold results.

Placeholders are written as `<...>`. A filled record contains none of them: an
unfilled placeholder is an unanswered question, and leaving one in place publishes
it as though it were an answer.

**Sections 1 to 4 are written before the run.** That ordering is the only thing this
template does that a results document does not. Registering the method and the
failure condition in advance is what separates a measurement from a search for a
number that supports what was already believed. If a section below was written after
the results were seen, say so in that section rather than presenting it as
pre-registered.

The section headings below are required and are checked by
[`tests/telemetry/`](../../../tests/telemetry/). Add sections freely; do not remove
one, and do not repeat one.

---

# `<work item>` experiment

Date registered: `<YYYY-MM-DD>`
Date executed: `<YYYY-MM-DD, or "not executed">`

## Classification and certification

Evidence class: `<local-static | mock | synthetic | estimated | local-real-cpu | cloud-real-cpu | cloud-real-gpu | documented-unexecuted>`

Ceiling this class carries: `<C0 | C1 | C2 | none>`, per
[the certification document](../../testing/certification.md).

Level this record actually supports: `<...>`. Pick the weakest label the record
supports, not the strongest one it could be read as.

Claim boundary: `<one paragraph: exactly what this establishes and what it does not.>`

## Provenance

| Input | Immutable identifier |
|---|---|
| Repository revision | `<git commit sha>` |
| Container image | `<name@sha256:...>` |
| Model artifact | `<repository, revision, per-file hash>` |
| Tooling | `<name and exact version, one row per tool>` |

A moving tag is not a provenance record. If an input cannot be pinned, say which one
and what that costs the result.

## Environment

| Property | Value |
|---|---|
| Host | `<make, CPU, instruction set extensions, cores>` |
| Memory and disk | `<total, and free at the start of the run>` |
| Operating system | `<name and build>` |
| Container engine | `<name and version>` |
| Cluster | `<distribution, version, node count, or "none">` |
| Accelerator | `<model and driver, or "none; CPU only">` |

Number of hosts this was executed on: `<...>`. If it is one, the record is true of
one machine, and every claim drawn from it inherits that.

## Method

**Registered before the run.**

Question: `<the single question this experiment answers>`

Hypothesis: `<what is expected, in a form that could turn out to be wrong>`

Procedure:

```text
<the exact commands, in order>
```

Pre-registered thresholds:

| ID | Threshold | Blocking? | Rationale |
|---|---|---|---|
| `<T1>` | `<the condition that counts as a pass>` | `<yes / no>` | `<why this is the line>` |

Failure condition: `<what result would make the hypothesis wrong, stated so that it
cannot be reinterpreted afterwards>`

Stop condition: `<what would end the run early — a budget, a timeout, a hazard>`

## Results

| Threshold | Verdict | Measured or observed | Note |
|---|---|---|---|
| `<T1>` | `<met / not met / not executed>` | `<value, with units>` | |

Every value is labelled **measured** or **derived**. A number produced by
calculation is derived however carefully it was calculated, and derived values
cannot certify.

Commands that failed, and their output:

```text
<paste, redacted. A failure that is omitted is the one the next person repeats.>
```

Unexpected observations: `<what happened that the method did not anticipate>`

## Limitations

- `<what this does not establish>`
- `<what varied between runs, and by how much>`
- `<what was not executed, and why>`
- `<which claims must not cite this record>`

## Authorisation

Required: `<yes / no>`, because `<cost, artifact size, or a resource outside the
contributor's own machine>`.

Granted by: `<who, and when — or "not obtained", with the stages that were therefore
not run>`.

Cleanup verified: `<how, and what was left behind>`.
