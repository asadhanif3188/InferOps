# V1-S0-001-PR1 governance validation

Date: 2026-08-23

Classification: local static documentation evidence

Claim boundary: repository governance only; not mock, synthetic, estimated, runtime,
deployment, security-certification, or production evidence

## Source and environment

- Target branch: `docs/v1-s0-001-repository-governance`
- Base commit: `2926eb95f84398ba0ac0846897f9644677b674eb`
- Operating system reported by the validation shell: Microsoft Windows NT
  `10.0.26200.0`
- Git: `2.45.1.windows.1`
- Windows PowerShell: `5.1.26100.8875`

These values describe the check environment only. They are not supported-runtime or
cross-platform certification.

## Commands and results

### Relative Markdown links

The check enumerated every `*.md` file, extracted Markdown link destinations,
excluded HTTP(S), `mailto:`, and same-document anchors, resolved each remaining path
relative to its source document, and failed when `Test-Path -LiteralPath` returned
false.

Result before this evidence file was added: `PASS (10 files)`.

Result after this evidence file was added: `PASS (11 files)`.

### External links

```powershell
$externalLinks = @(
  'https://keepachangelog.com/en/1.1.0/',
  'https://semver.org/spec/v2.0.0.html'
)
foreach ($externalLink in $externalLinks) {
  Invoke-WebRequest -Uri $externalLink -Method Head -UseBasicParsing `
    -MaximumRedirection 5 -ErrorAction Stop
}
```

Result: both references returned HTTP `200` on 2026-08-23.

The initial restricted-network attempt failed with a PowerShell
`WebCmdletWebResponseException` and no status code. The same check was rerun with
explicit network authorization and succeeded. No failed result is represented as
proof.

### Repository checks

```text
git diff --check
rg -n "[ \t]+$" README.md CONTRIBUTING.md CHANGELOG.md \
  CODE_OF_CONDUCT.md LICENSE docs .github
```

Results:

- Git whitespace check: pass.
- Changed-file trailing-whitespace scan: pass (no matches).
- Relative documentation links: pass.
- Credential, private-source, personal-path, and generated-artifact review: pass;
  no findings in the public diff.
- Scope review: documentation/governance only; no product code, contract, ADR,
  runtime configuration, model artifact, or capability implementation present.

Restricted-source comparison inputs and identifiers are intentionally not reproduced
in this public record.

## Limitations and skipped checks

- No repository-approved Markdown linter or link-checking tool exists yet, and none
  was installed for this PR. The relative-link check used the recorded PowerShell
  environment; external links were checked separately.
- No CI workflow exists, so this evidence is local and not a remote required check.
- Remote branch protection, issue labels, review enforcement, and private reporting
  channels were not configured or tested.
- No runtime, model, container, Kubernetes, schema, API, telemetry, cost, or security
  experiment was authorized or performed.
- Legal review of the MIT license was not performed.
